; Copy to `secrets.ini` and fill in. `secrets.ini` is gitignored.
; Generate a token with: openssl rand -hex 16

[secrets]
build_flags =
    -DWIFI_SSID=\"changeme\"
    -DWIFI_PASS=\"changeme\"
    -DEPD_TOKEN=\"changeme\"
